[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "neddexbot"
version = "0.1.0"
description = "Neddex Discord vehicle collection bot"
requires-python = ">=3.12"
dependencies = [
    "discord.py",
]

[tool.setuptools.packages.find]
where = ["src"]